#!/usr/bin/env python


import lib.tmdb
from lib import tmdb_addon
from tmdb import *
from lib.functions import *
import datetime

"""
movi=getMovieInfo(2102)


#countries_list=[]
#for country in movi['countries']: countries_list.append((movi['countries'][country]).keys()[0])
#print ", ".join(outp)

genre_list=[]

genres=movi['categories']['genre'].keys()

for genre in genres:
   genre_list.append(genre)

genre_list=", ".join(genre_list)

print genre_list
"""

print datetime.date.today()
